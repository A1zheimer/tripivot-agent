# Python
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.venv/
.venv-hpc/
dist/
build/
*.egg-info/

# macOS / 编辑器
.DS_Store
.claude/

# Agent 运行时状态与本地运行产物
.translation-agent/
outputs/
runs/
logs/

# 模型与数据集缓存（run_qwen_hpc.sh 默认 HF_HOME=.cache/huggingface）
.cache/
*.safetensors
*.gguf

# 原始下载与蒸馏中间缓存：可离线重建，见 data/README.md
data/raw/

# 成品语料入库（中文单语 / zh-en / zh-my / ALT / glossary* / summary/manifest）。
# 只排除 Agent 运行时 SQLite 状态。
data/processed/*.sqlite3

# 可由 data/processed 重新打包，不入库；记录 SHA-256 的 JSON manifest 仍然入库
artifacts/*.tar.gz

# 汇报快照与可再生成的打包产物不入库
deliverables/
scripts/overnight_upload_modelscope.py  # contains ModelScope token

# local model artifacts (large, not for git)
demo/base/
demo/adapter_opd/
demo/adapter_grpo/
demo/models/
demo/server.log
demo/cache.json
docs/defense_ppt_material.md  # private defense prep
